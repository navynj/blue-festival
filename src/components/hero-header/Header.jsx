import React from 'react';
import styles from '../../styles/hero-header/Header.module.css';

const Header = () => {
  return (
    <header>
      <h1>
        Blue <br /> Festival
      </h1>
      <p>Where the blues come alive, experience the blues like never before.</p>
    </header>
  );
};

export default Header;
